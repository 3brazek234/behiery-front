import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["admin.behiryperfume.com", "behiryperfume.com", "test.behiryperfume.com"],
  },
};

export default nextConfig;
